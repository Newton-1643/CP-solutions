#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, m; //n people in m photos
    cin >> n >> m;

    vector <int> v(n);
    for (int i = 0; i < m; i++)
    {
        for (int j = 0; j < n; j++)
        {
            cin >> v[j];

            if (j != 0 && v[j] )
        }
    }
}